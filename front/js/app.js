
// Build the Clouds Scene
const CloudsScene = new ScrollMagic.Scene({
  duration: TrainAnimationDuration,
})
  .setTween(Clouds)
  .addIndicators();

  CloudsScene,
  Snow,
  BackMoutainScene,
  FrontHousesScene,
  NotificationScene,
  SectionOneScene,
  SectionTwoScene,
]);
